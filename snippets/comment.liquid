<!-- /snippets/comment.liquid -->
{% comment %}

This snippet generates the structure of a comment.
It is used to show not only recent comments, but also comments that were
just published but not yet approved.

More information:
- http://docs.shopify.com/themes/liquid-variables/comment

{% endcomment %}

<!--<div class="comment">
<div class="rte">
{{ comment.content }}
</div>

<span>
{% capture author %}<strong>{{ comment.author }}</strong>{% endcapture %}
{% capture date %}<strong><time datetime="{{ comment.created_at | date: '%Y-%m-%d' }}">{{ comment.created_at | date: format: 'month_day_year' }}</time></strong>{% endcapture %}
{{ 'blogs.article.comment_meta_html' | t: author: author, date: date }}
</span>
</div> --> 
<div class="single-reply">
  <div class="s-text8 p-b-40">
    <div class="comment-author" style="float: left;padding-right: 20px;">
      <img alt="" src="{{ 'avatar.png' | asset_url }}">
    </div>
    <div class="">
      <a href="#"><b>{{ comment.author }}</b></a>
      /
      <span><time datetime="{{ comment.created_at | date }}">{{ comment.created_at | date: format: 'month_day_year' }}</time></span>
      <a href="#">{{ 'blogs.comments.reply' | t }}</a>
    </div>
    <p>{{ comment.content }}</p>
  </div>
</div>